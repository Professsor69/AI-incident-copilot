name: CI

on:
  push:
    branches: ["main", "develop", "feature/**"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    name: Lint (Ruff)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: "2.4.1"
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Cache Poetry virtualenv
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            venv-${{ runner.os }}-

      - name: Install dev dependencies
        run: poetry install --with dev --no-root

      - name: Run Ruff linter
        run: poetry run ruff check . --output-format=github

      - name: Run Ruff formatter check
        run: poetry run ruff format --check .

  type-check:
    name: Type Check (mypy)
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: "2.4.1"
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Cache Poetry virtualenv
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            venv-${{ runner.os }}-

      - name: Install dev dependencies
        run: poetry install --with dev --no-root

      - name: Run mypy type checker
        run: poetry run mypy api/ agent/ rag/ eval/ --ignore-missing-imports

  # NOTE: The eval-harness job is added in Phase 9 once the evaluation
  # harness is built. It will fail the build if accuracy < 85%.
  # eval-harness:
  #   name: Eval Harness (Accuracy Gate)
  #   runs-on: ubuntu-latest
  #   needs: [lint, type-check]
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - ...
  #     - name: Run evaluation harness
  #       run: poetry run python eval/run_evals.py --fail-threshold 0.85
